# AI Padel Performance Analysis System

This cloud-deployable computer vision engine uses object tracking and velocity math parameters to analyze structural padel parameters directly inside a Streamlit web canvas environment.

## Execution and Installation Local Setup

1. Clone or copy these repository structures locally.
2. Install the necessary dependencies directly via standard pip terminals:
```bash
pip install -r requirements.txt